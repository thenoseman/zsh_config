update_important_tools() {

  BREW_PACKAGES=(
  autojump
  colordiff
  ctags
  diff-so-fancy
  direnv
  exiftool
  ffmpeg
  flvstreamer
  fzf
  git
  imagemagick
  jhead
  macvim
  node
  nvm
  openssl
  pstree
  python
  rbenv
  readline
  redis
  ruby-build
  shellcheck
  sqlite
  tig
  unrar
  watch
  yarn
  youtube-dl
  zsh
  burntsushi/ripgrep/ripgrep-bin
  jq
  )

  BREW_CASKS=(
  kdiff3
  macpass
  )


  NODE_PACKAGES=(
  babel-eslint
  eslint
  gulp
  tslint
  typescript
  webpack
  )

  INSTALLED_PACKAGES=$(brew list ${BREW_PACKAGES})

  # If brew list fails there are missing packages
  if [[ "$?" != "0" ]]; then
    echo "${ccyellow}Installing missing packages${ccend}"
    brew tap burntsushi/ripgrep https://github.com/BurntSushi/ripgrep.git
    brew tap caskroom/cask
    brew install ${BREW_PACKAGES}
    brew cask install ${BREW_CASKS}
  fi

  echo "${ccyellow}Updating rubygems${ccend}"
  gem update --system

  echo "${ccyellow}Installing important gems${ccend}"
  gem install --silent pry pry-byebug bundler bond wirb haml-lint

  echo "${ccyellow}Installing important JS tools${ccend}"
  yarn global add ${NODE_PACKAGES} --prefer-offline --no-lockfile -s

  echo "${ccyellow}Checking homebrew update (updating if more than 3 packages updated)${ccend}"
  brew update
  UPDATES=$(brew outdated | wc -l)
  [[ "${UPDATES}" -ge "3" ]] && brew upgrade
}
# vim:ft=sh
