# gem install pry pry-doc pry-debugger
#
# put this at the end of config/boot.rb:
# begin; load File.expand_path("~/.railsboot"); rescue Exception; end
#
%w[pry.rb pry-debugger.rb slop.rb method_source.rb coderay.rb].each { |a| $LOAD_PATH << File.dirname(Gem.find_files(a).first) }

require "pry"
require "pry-debugger"

# vim: set ft=ruby
